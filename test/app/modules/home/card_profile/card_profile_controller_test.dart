import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:elis/app/modules/home/card_profile/card_profile_controller.dart';
import 'package:elis/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // CardProfileController cardprofile;
  //
  setUp(() {
    //     cardprofile = HomeModule.to.get<CardProfileController>();
  });

  group('CardProfileController Test', () {
    //   test("First Test", () {
    //     expect(cardprofile, isInstanceOf<CardProfileController>());
    //   });

    //   test("Set Value", () {
    //     expect(cardprofile.value, equals(0));
    //     cardprofile.increment();
    //     expect(cardprofile.value, equals(1));
    //   });
  });
}
