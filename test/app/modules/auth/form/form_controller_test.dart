import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:elis/app/modules/auth/form/form_controller.dart';
import 'package:elis/app/modules/auth/auth_module.dart';

void main() {
  initModule(AuthModule());
  // FormController form;
  //
  setUp(() {
    //     form = AuthModule.to.get<FormController>();
  });

  group('FormController Test', () {
    //   test("First Test", () {
    //     expect(form, isInstanceOf<FormController>());
    //   });

    //   test("Set Value", () {
    //     expect(form.value, equals(0));
    //     form.increment();
    //     expect(form.value, equals(1));
    //   });
  });
}
