import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:elis/app/modules/case/case_controller.dart';
import 'package:elis/app/modules/case/case_module.dart';

void main() {
  initModule(CaseModule());
  // CaseController case;
  //
  setUp(() {
    //     case = CaseModule.to.get<CaseController>();
  });

  group('CaseController Test', () {
    //   test("First Test", () {
    //     expect(case, isInstanceOf<CaseController>());
    //   });

    //   test("Set Value", () {
    //     expect(case.value, equals(0));
    //     case.increment();
    //     expect(case.value, equals(1));
    //   });
  });
}
